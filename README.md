# 🧮 WPF Calculator App

> **WPF Calculator App** — это полноценный **настольный калькулятор**, разработанный на **C#** с использованием фреймворка **Windows Presentation Foundation (WPF)**. Приложение имеет интуитивно понятный графический интерфейс и поддерживает базовые математические операции.

![Картинка](https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTViHaVL9gXtRnhinKEOcz4TNeTejwdFC_TcQ&s)
---

## 💻 Установка и Запуск
### Необходимые компоненты

Для успешной сборки и запуска проекта вам потребуется:

1.  **Платформа .NET** (Рекомендуется **.NET 6.0 Desktop Runtime** или выше).
2.  **Инструменты разработки**:
    * **Visual Studio** (версия 2019 или 2022) с установленной рабочей нагрузкой **"Разработка классических приложений .NET"**.
3.  Операционная система **Windows** (WPF является технологией, специфичной для Windows).

***~~Обратите внимание: это не кроссплатформенное приложение.~~***

### Пошаговая инструкция

1.  **Клонируйте** репозиторий на ваш компьютер:
    ```bash
    git clone https://github.com/zvshkin/WPF-Calculator-App.git
    cd WPF-Calculator-App
    ```
2.  **Откройте** файл `.sln` (решение) в **Visual Studio**.
3.  **Соберите** проект (Меню -> Build -> Build Solution).
4.  **Запустите** приложение (Нажмите **F5**).

---

## 🛠️ Особенности Интерфейса и Кода

### Основные Элементы Управления

В приложении реализованы следующие элементы интерфейса:

| Элемент WPF | Назначение | Примечания |
| :--- | :--- | :--- |
| `TextBox` (Display) | Отображение ввода и результата. | Использует свойство `TextAlignment="Right"`. |
| `Button` | Числа и операторы. | Стиль кнопок задан в `App.xaml`. |
| `Grid` | Организация элементов на форме. | Основной контейнер. |

### Вставки кода (Обработка Нажатия Кнопки)

Ниже представлен пример обработчика событий на C# для кнопок с цифрами, демонстрирующий _логику добавления числа_ в отображаемое поле:

```csharp
// Обработчик события Click для всех цифровых кнопок
private void NumberButton_Click(object sender, RoutedEventArgs e)
{
    Button b = (Button)sender;
    string digit = b.Content.ToString();
    
    // Предотвращение добавления нескольких нулей в начале
    if (DisplayTextBox.Text == "0" && digit != ".")
    {
        DisplayTextBox.Text = digit;
    }
    else
    {
        DisplayTextBox.Text += digit;
    }
}
```
---

Автор проекта: zvshkin

GitHub: https://github.com/zvshkin
